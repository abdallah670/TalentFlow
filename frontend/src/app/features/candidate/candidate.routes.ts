import { Routes } from '@angular/router';

/**
 * Candidate portal routes.
 * Pages are generated from design mockups; routes will be wired up
 * as the candidate portal is connected to the app shell.
 */
export const CANDIDATE_ROUTES: Routes = [];
